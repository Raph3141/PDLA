package model;
import model.*
public class HelpSeekers extends Users{
    public HelpSeeker (String id,String firstName,String lastName,String email,String password){
        super (id, firstName, lastName,email,password);
    }
}

